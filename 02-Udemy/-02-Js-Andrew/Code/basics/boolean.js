// === - equality operator
// !== - not equal operator
// < - less than operator
// > - greater than operator
// >= - greater than or equal to operator
// <= - less than or equal to operator

let temp = 80;

if (temp <= 32) {
  console.log("It is freezing outside!");
}

if (temp >= 110) {
  console.log("It is way too hot outside!");
}

// CHALLENGE AREA

// Create age set to my age
let age = 25;

// // Calculate is child - if they are 7 or under
// let isChild = age <= 7;

// // Calculate is senior - if they are 65 or older
// let isSenior = age >= 65;

// // Print is child value
// console.log(isChild);

// // print is senior value
// console.log(isSenior);

if (age <= 7) {
  console.log("You are gonna get the Children's discount");
}

if (age >= 65) {
  console.log("You are gonna get the Senior's discount");
}
